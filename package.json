{
    "name": "coach-me",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "prettier": "prettier --single-quote --write"
    },
    "devDependencies": {
        "husky": "^3.0.9",
        "lint-staged": "^9.4.2",
        "prettier": "^1.18.2"
    },
    "lint-staged": {
        "*.{js,json,scss,css}": [
            "yarn prettier",
            "git add"
        ]
    }
}
